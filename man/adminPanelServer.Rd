% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_panel_server.R
\name{adminPanelServer}
\alias{adminPanelServer}
\title{Admin Panel Server Module}
\usage{
adminPanelServer(id, conAnn, pass = Sys.getenv("STMP_PASS"))
}
\arguments{
\item{id}{Character string. The module ID, must match the UI module.}

\item{conAnn}{DBI connection object. Database connection for storing user data.}

\item{pass}{Character string. SMTP password from environment.
Defaults to \code{Sys.getenv("STMP_PASS")}.}
}
\value{
Invisibly returns NULL (typical for Shiny server modules).
}
\description{
Server-side logic for the admin panel. Manages user creation, deletion,
password generation, and email notification functionality.
}
\details{
This module provides:
\itemize{
\item User creation with role assignment
\item Automatic password generation
\item Email notification for new users
\item User deletion with admin protection (prevents deleting last admin)
\item User listing in interactive data table
}

Database Requirements:
The annotation database should contain a \code{users} table with columns:
\itemize{
\item user (character)
\item email (character, nullable)
\item password (character, hashed)
\item role (character: 'user' or 'admin')
}
}
\section{Dependencies}{

Requires packages: shiny, DBI, sodium, blastula, DT
}

\examples{
\dontrun{
# In a Shiny app
shiny::moduleServer("admin_panel", adminPanelServer,
  args = list(
    conAnn = con_annotation,
    pass = Sys.getenv("SMTP_PASS")
  )
)
}
}
