% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createAnalysisModule.R
\name{createAnalysis}
\alias{createAnalysis}
\title{Analyse-Erstellungs-Modul Server}
\usage{
createAnalysis(
  id,
  conDMW = conDmw,
  conAnn = conAnn,
  task = NULL,
  user = user_name()
)
}
\arguments{
\item{id}{Character. Modul-ID, muss mit der UI-Funktion übereinstimmen.}

\item{conDMW}{DBI-Verbindungsobjekt. Datenbankverbindung für DMW-Transkriptionsdaten.}

\item{conAnn}{DBI-Verbindungsobjekt. Datenbankverbindung für Analysedaten.}

\item{task}{Character oder NULL. Optionale vorausgewählte Aufgabe (\code{rwl}-Wert).
Wird aktuell nicht ausgewertet; die Aufgabe wird reaktiv über das UI gewählt.}

\item{user}{Character. Benutzername des aktuell angemeldeten Nutzers.}
}
\value{
Gibt unsichtbar \code{NULL} zurück (typisch für Shiny-Server-Module).
}
\description{
Server-seitige Logik für das Modul zur Verwaltung von Kartenanalysen.
Steuert das Erstellen, Laden und Löschen von Analysen sowie die interaktive
Zuordnung von IPA-Tokens zu Kategorien per Drag-and-Drop.
}
\details{
Das Modul führt folgende Aufgaben aus:
\itemize{
\item Laden aller verfügbaren \code{rwl}-Werte aus der Transkriptionsdatenbank
\item Erstellen einer neuen Analyse inkl. optionaler Startkategorien (Modal)
\item Laden einer bestehenden Analyse (eigene oder freigegebene)
\item Löschen einer Analyse (noch nicht vollständig implementiert)
\item Sichtbarkeitssteuerung: Analyse für andere Nutzer freigeben/sperren
\item Kategorienverwaltung: Hinzufügen, Umbenennen und Löschen von Kategorien
(mit kaskadierten Updates in \code{mapAnalysisCategory} und \code{mapAnalysisAnalysis})
\item Drag-and-Drop-Sortierung von IPA-Tokens zwischen Kategorien via
\code{sortable::bucket_list}; Änderungen werden sofort in die Datenbank geschrieben
\item Filterung der anzuzeigenden Tokens nach Silbenposition und IPA-Muster
(clientseitig über \code{apply_multiple_filters})
}
}
\section{Datenbank-Anforderungen}{

Die DMW-Datenbank (\code{conDMW}) sollte folgende Tabelle enthalten:
\itemize{
\item \code{Transcription}: Spalten \code{rwl} und \code{ipa}
}

Die Annotationsdatenbank (\code{conAnn}) sollte folgende Tabellen enthalten:
\itemize{
\item \code{mapAnalysis}: \code{id}, \code{name}, \code{rws}, \code{user}, \code{visibileToOthers}
\item \code{mapAnalysisCategory}: \code{analysis_id}, \code{category_name}, \code{created_by}
\item \code{mapAnalysisAnalysis}: \code{analysis_id}, \code{rws}, \code{ipa}, \code{category}, \code{changed_by}
}
}

\section{Dependencies}{

Benötigt die Pakete: shiny, DBI, dplyr, glue, shinyWidgets, sortable
}

\examples{
\dontrun{
# In einer Shiny-App
createAnalysis("createAnalysis", con_dmw, con_annotation, user = "nutzer1")
}
}
